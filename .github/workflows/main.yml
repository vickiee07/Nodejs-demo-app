name: CI/CD Pipeline
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build Docker image
        run: docker build -t  vickieee07 /app:jsn -f Dockerfile .

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: vickiee007
          password: dckr_pat_v3Z3kDx7vGwKRcVx_6ZqWO4my5M

      - name: Push Docker image
        run: docker push$ vickiee07/app:jsn

      
